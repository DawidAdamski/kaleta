---
artboard: 2a
views_hash: 02bd3aff1aec7476
shot_at: 2026-09-21
---

# Fidelity report - 2a Transactions

Spec: [`artboards/2a.html`](../artboards/2a.html). Pictures: `.fidelity/2a/index.html` after `shoot 2a`.

Read at 1360x900, mini drawer, seeded demo ledger, grouped by week with
three rows ticked (`_prepare_ledger`). Artboard values are the inline ones in
`2a.html`; app values are `getComputedStyle` off the running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Page column | padding 32px 36px 40px, 20px gap | `padding=32px 36px 40px; gap=20px` | match | `.nicegui-content`'s own 1rem is zeroed under `.k-page`. |
| 2 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656`, 6px above the title | `fontSize=10px; fontWeight=600; letterSpacing=2px; color=rgb(110,102,86); textTransform=uppercase` | match | Wording is the ledger's own: "N movements, M accounts, one currency" rather than the artboard's sample sentence. |
| 3 | Page title | `300 32px/1`, `-.02em`, `#1C1A15` | `fontSize=32px; fontWeight=300; letterSpacing=-0.64px; color=rgb(28,26,21)` | match | |
| 4 | Title-row primary action | `9px 16px` on `#1C1A15`, 999px, icon 17px `#F3EFE7`, label `600 12.5px` `#F3EFE7` | `minHeight=36px; padding=0 14px; borderRadius=999px; backgroundColor=rgb(28,26,21); color=rgb(243,239,231); fontSize=12.5px; fontWeight=600` | match | 36px against the artboard's 9+9+18: a Quasar button sets its height, and 36 is the pill height every other title row in the app carries. |
| 5 | Keyboard hint inside it | `400 10.5px` mono `#A8A08D`, `1px 5px`, 1px `#4A443A` box, radius 4 | `fontSize=10.5px; color=rgb(181,171,150); padding=1px 5px; borderRadius=4px` on a 1px `--k-on-ink-muted` outline | match | `Alt+N`, which is the shortcut the app actually binds; the artboard writes the Mac glyph. |
| 6 | Toolbar card | `#FCFAF6`, radius 12, `14px 16px`, shadow `0 1px 2px rgba(28,26,21,.05)`, 8px gap | the same card, `k-toolbar-card` | match | |
| 7 | Filter chip, set | `6px 12px` on `#F3EFE7`, 999px, `400 12.5px` `#1C1A15`, 15px chevron | `padding=6px 12px; borderRadius=999px; fontSize=12.5px` | match | |
| 8 | Filter chip, unset | `6px 12px`, 1px dashed `#CFC5AE`, 999px, `400 12.5px` `#6B6353` | same, `--k-chip-dash` = `#CFC5AE` | match | |
| 9 | Selection bar | `11px 18px` on `#EFE3D6`, radius 10, 14px gap, a 1x16 `#D6C6B2` rule between actions | `k-selection-bar` on `--k-surface-warm-strong`, `k-selection-divider` 1x16 on `--k-border-strong` | match | Delete carries its word, not only its icon; the close is the quiet action at the end. |
| 10 | Ledger card | `#FCFAF6`, radius 12, `padding:8px 0 0`, shadow `.05`, overflow hidden | `backgroundColor=rgb(252,250,246); borderRadius=12px; padding=8px 0px 0px; boxShadow=rgba(28,26,21,.05) 0 1px 2px` | match | |
| 11 | Table header | `600 10px/.14em` uppercase `#6E6656`, `8px 18px 12px`, 1px `#E2DBCC` under | `fontSize=10px; fontWeight=600; letterSpacing=1.4px; color=rgb(110,102,86); textTransform=uppercase; borderBottom=1px solid rgb(226,219,204)`, row 39px | match | Quasar fixes `thead tr` at 48px; `height:auto` is what brings it to the artboard's 39. |
| 12 | Table columns | grid `38px 84px 150px 1fr 168px 84px 118px 130px 76px` | Quasar sizes them from content; order, alignment and the right-aligned amount are the artboard's | deviation | `ui.table` has no column-width spec of that shape. What the artboard is saying - which columns, in which order, and that the amount is the only right-aligned one - is exact. |
| 13 | Table row | 13.5px `#1C1A15`, 1px `#EDE7DA` under | `height=35.25px; fontSize=13.5px; borderBottom=1px solid rgb(237,231,218); paddingTop/Bottom=7px` | match | Quasar's fixed 48px `td` had to go to `height:auto`, and the checkbox's 40px reservation to 18px, or a row could not be 35. |
| 14 | Group label row | `7px 18px` on `#F6F1E7`, 1px `#EDE7DA` under, `500 11px/.04em` `#4A443A` | the same band, written by `attach_group_labels` into the row stream | match | The label reads "Week of 14 September" in the genitive (`common.month_of_N`), which is the only grammatical form for a day plus a month in Polish. |
| 15 | Row checkbox | 15x15, 1.5px `#8E8676`, radius 3 | 18px `.q-checkbox__inner`, `color=info` so the tick is ink on paper rather than Quasar's primary | deviation | Quasar draws its own checkbox; 15px is below its smallest, and `!important` colour helpers mean the tick is set by redefining `--q-info` on the card. |
| 16 | Card footer | 1px `#E2DBCC` over, `#6B6353` 12px labels, page-size select and the pager | `borderTop=1px solid rgb(226,219,204); paddingTop=14px`, `k-ledger-foot` | match | The pager is a `k-segment`; the page size a sunken pill. |
| 17 | Segmented control | `#F3EFE7`, radius 8, 2px padding; chosen `5px 12px` on `#1C1A15`, radius 6, `500 12px` `#F3EFE7` | `backgroundColor=rgb(243,239,231); borderRadius=8px; padding=2px`; button `minHeight=26px; padding=0 12px; borderRadius=6px` | match | Quasar's `.bg-primary`/`.text-white` are `!important`: the chosen one goes ink by redefining `--q-primary` and `--q-info` on `.k-segment`, with `toggle-text-color=info`. |
| 18 | Export button | a second pill on the title row, paper on `#E2DBCC` | not present | deviation | The app has no ledger export. Adding one is new behaviour, which this plan's Scope puts out of bounds. |
| 19 | Numbers | IBM Plex Mono, tabular, amount right-aligned and signed | `k-mono` with `font-variant-numeric:tabular-nums`; income/expense tones from `amount_class` | match | |
| 20 | Sample data | Polish payees, tags and a full month | the same, from `scripts/seed.py` - the shoot runs the rich seed rather than `DataService.seed`, which has no payees, tags or planned rows | match | |
