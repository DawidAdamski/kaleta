---
artboard: 3d
views_hash: a71d558a61b59fd0
shot_at: 2026-09-21
---

# Fidelity report - 3d Financial Wizard

Spec: [`artboards/3d.html`](../artboards/3d.html). Pictures: `.fidelity/3d/index.html` after `shoot 3d`.

Read at 1360x900, mini drawer, the seeded ledger with the Setup section
clicked open (`_prepare_wizard_setup_open`). Artboard values are the inline
ones in `3d.html`; app values are `getComputedStyle` off the running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Page column | padding 36px 40px 44px, 26px gap | `padding=36px 40px 44px; gap=26px` | match | This page breathes like the dashboard, two pixels tighter. |
| 2 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656`, 6px above | `fontSize=10px; letterSpacing=2px; color=rgb(110,102,86)`, reading "Setup complete - 1 suggestion waiting" | match | Two facts, in the order the page answers them: whether the ledger is set up at all, and whether anything is asking for a decision today. |
| 3 | Page title | `300 32px/1`, `-.02em` | `fontSize=32px; fontWeight=300` | match | |
| 4 | Subtitle | `400 13.5px/1.6` `#4A443A`, 12px under, in a 640px block | `13.5px` at `leading-[1.6]` in `--k-ink-2`, the hero column `max-w-[640px]` | match | Wording is the app's own. |
| 5 | Mentor card | `#FCFAF6`, radius 12, `24px 26px`, shadow `.05`, 3px `#B4591F` left rule, 20px gap | `SECTION_CARD_FEATURE` + `k-accent-rule`: `padding=24px 26px; borderRadius=12px; borderLeft=3px solid rgb(180,89,31)` | match | The padding was `!p-0` with a `px-[26px] py-6` row inside it, which is the same figure written where nothing else could reuse it. |
| 6 | Its lamp | 26px `#B4591F`, 2px down | `ui.icon("lightbulb", size="26px")` in `--k-accent-text`, `mt-0.5` | match | |
| 7 | Its eyebrow | `600 10px/.2em` uppercase `#8E4718` reading "Suggested next" | `k-mentor-eyebrow`: `fontSize=10px; letterSpacing=2px; color=rgb(142,71,24)` | match | |
| 8 | Its title and body | title `500 18px` `#1C1A15` 6px under; body `400 13px/1.6` `#4A443A`, 660px wide, 6px under | `text-lg font-medium mt-1.5` and `text-[13px] leading-[1.6] max-w-[660px]` | match | |
| 9 | Its two buttons | `9px 16px` ink radius 8 with a 16px arrow, `600 12.5px`; beside it `9px 14px` on 1px `#CFC5AE`, radius 8, `500 12.5px` `#4A443A`; 10px apart, 16px under | `k-btn-ink`: `padding=0 14px; borderRadius=8px; backgroundColor=rgb(28,26,21); color=rgb(243,239,231); fontSize=12px; fontWeight=600`; `k-btn-outline`: `border=1px solid rgb(207,197,174); borderRadius=8px; fontSize=12.5px; color=rgb(74,68,58)` | match | |
| 10 | Section head | eyebrow `600 10px/.22em` uppercase `#1C1A15`, a `400 12px` `#6B6353` note 12px away, `padding-bottom:14px`, 1px `#DCD4C2` under | `k-section-rule`: `paddingBottom=14px; borderBottom=1px solid rgb(220,212,194); gap=12px`; `k-section-rule-title`: `fontSize=10px; fontWeight=600; letterSpacing=2.2px; color=rgb(28,26,21)` | match | Neither section is a card: an eyebrow over one rule, with what it heads on the ground under it. |
| 11 | Setup head wording | "Setup" and "All four done - collapsed by default once complete" | "Setup" and "All four done" | deviation | The second half of the artboard's line is a note to the reader of the artboard, not type for the page. |
| 12 | Setup, open or closed | drawn open, with a `keyboard_arrow_up` at the right | collapsed once all four are ticked, open while they are not; the chevron follows | deviation | The artboard annotates the collapse and still draws the open state, so the shoot clicks it open rather than the page changing its default. |
| 13 | Setup grid | four columns, 16px apart, 16px under the rule | `grid-cols-2 md:grid-cols-4`, `gap:16px`, `mt-4` | match | Two columns below `md`, where four 292px cards do not fit. |
| 14 | Setup card | `#FCFAF6`, radius 12, `18px 20px`, shadow `.05` | `backgroundColor=rgb(252,250,246); borderRadius=12px; padding=18px 20px; boxShadow=rgba(28,26,21,.05) 0 1px 2px` | match | |
| 15 | Its tick | 20px `#36684D` disc, 14px `#FCFAF6` check, 9px from the name | `width=20px; height=20px; borderRadius=999px; backgroundColor=rgb(54,104,77)` | match | A tick, not the step's own glyph: on a done card the only thing worth a mark is that it is done. |
| 16 | Its three lines | name `500 13.5px` `#1C1A15`; count `400 12px` `#6B6353` 8px under; way in `500 12px` `#9A4E1F` 10px under | the same three, `k-link-action`: `fontSize=12px; fontWeight=500; color=rgb(154,78,31)` | match | The counts are terse and plural-aware, so Polish reads "4 konta" and not "4 kont"; the import card's way in says "Import more", because that step is never finished. |
| 17 | Routines head | "Routines" and "5 ready - 8 not built yet" | the same shape, reading "13 ready - 0 not built yet" | deviation | Every routine the artboard greys out has since been built. |
| 18 | Routines grid | two columns, `gap:0 44px` | `grid-cols-1 md:grid-cols-2` with `row-gap:0; column-gap:44px` | match | |
| 19 | Routine row | 14px gap, `15px 0`, 1px `#E7E0D0` under; 20px glyph `#4A443A`; title `500 14px`; description `400 11.5px` `#6B6353` 2px under; "Open" `500 12px` `#9A4E1F` | `padding=15px 0px; borderBottom=1px solid rgb(237,231,218); gap=14px`; `k-routine-desc`: `fontSize=11.5px; color=rgb(107,99,83); marginTop=2px` | deviation | `#EDE7DA`, not `#E7E0D0`: the hairline is the app's row rule and the artboards use it 123 times against this one's 12. |
| 20 | Description length | one terse line per routine | the app's own sentence, clamped to one line (`white-space:nowrap`, ellipsis), with the whole of it and the section on the row's tooltip | deviation | The artboard reads the index at a glance; a two-line paragraph on each of thirteen rows makes a page you have to read rather than scan. Rewriting thirteen descriptions into new copy is not a restyle. |
| 21 | "Not built" label | `400 10.5px/.1em` uppercase `#8E8676` at the row's end, the row's type greyed | the same label and greying, on any routine with no route behind it | match | Nothing on this ledger is unbuilt, so the state is not in the picture. |
| 22 | Footnote | none | an `info_outline` line explaining the "Not built" label | deviation | Drawn only while there is a not-built step to explain, so on this ledger it is absent - which is why the artboard has no row for it. |
| 23 | Colour | ink, muted, `#9A4E1F` on every way in, `#36684D` on the ticks | `--k-ink`, `--k-muted`, `--k-accent-text`, `--k-income` | match | |
