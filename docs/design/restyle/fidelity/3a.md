---
artboard: 3a
views_hash: 4d568ed3e34f914b
shot_at: 2026-09-21
---

# Fidelity report - 3a Forecast

Spec: [`artboards/3a.html`](../artboards/3a.html). Pictures: `.fidelity/3a/index.html` after `shoot 3a`.

Read at 1360x900, mini drawer, the baseline run the page starts on its
own. Artboard values are the inline ones in `3a.html`; app values are
`getComputedStyle` off the running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Page column | padding 32px 36px 40px, 22px gap | `padding=32px 36px 40px; rowGap=22px` | match | `PAGE_GAP_22`, the class `2b` and `3e` already take. The artboards draw the page column at 20, 22 and 24, and the app has one class for each; this page had simply not asked for its own. |
| 2 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656`: account, history, horizon | `fontSize=10px; letterSpacing=2px; color=rgb(110,102,86); textTransform=uppercase`, reading "All Accounts - 165 days of history - 60-day horizon" | match | What was asked, on the line above what came back. The status line that used to sit under the title is this eyebrow now. |
| 3 | Page title | `300 32px/1`, `-.02em`, reading "Forecast" | `fontSize=32px; fontWeight=300; letterSpacing=-0.64px` | match | It read "Balance Forecast"; the drawer and the header already say which screen this is. |
| 4 | Account picker | `8px 13px` on `#FCFAF6`, 1px `#E2DBCC`, radius 8, `400 12.5px`, a 15px `#6B6353` chevron | `k-select-pill--square`: `minHeight=32px; borderRadius=8px; backgroundColor=rgb(252,250,246)` | match | |
| 5 | Horizon control | a 2px-padded box on `#FCFAF6` with 1px `#E2DBCC`; three mono pills `5px 11px`, the chosen one ink radius 6 | `k-segment`: `backgroundColor=rgb(243,239,231); borderRadius=8px; padding=2px`; chosen: `backgroundColor=rgb(28,26,21); color=rgb(243,239,231); fontWeight=500; fontSize=12px` | deviation | Sunken sand, not paper on a hairline: `SEGMENT` is the app's one segmented control and `2a`, `2b` and `3e` all draw it that way. |
| 6 | Model control | Conservative / Baseline / Optimistic, the same box | present only when Prophet is installed, and it is not in the shoot | deviation | `KAL-FCT` already records that the preset selector is Prophet-only, and a test asserts it is hidden without Prophet. |
| 7 | Re-run | none | a paper pill on the title row | deviation | The artboard runs on load, which the app does too - but a control change re-runs after a delay, and `stale_action` needs a button to point at when it cannot. |
| 8 | Four stat cards | `flex:1`, `#FCFAF6`, radius 12, `20px 22px`, shadow `.05`, 18px apart | `padding=20px 22px; borderRadius=12px; backgroundColor=rgb(252,250,246); boxShadow=rgba(28,26,21,.05) 0 1px 2px`, `gap-[18px]` | match | They carried a round accent glyph and a dated hint line; on a 293px card that third line is what pushed a seven-figure balance onto two. |
| 9 | Stat figure | `500 28px` mono, tabular, 6px under the eyebrow; the change in `#36684D` | `fontSize=28px; fontWeight=500; fontFamily="IBM Plex Mono"; marginTop=6px` | match | |
| 10 | What a figure is as of | not drawn | on the card as `data-as-of`, and on the figure's tooltip | deviation | History ends at the last transaction, so on a quiet account "today" is not today. The fact stays on the element - a reader and a test can both read it - without a third line on the card. |
| 11 | Stat labels | "Balance today", "Predicted in 30 days", "Change over 30 days", "Confidence at day 60" | the same four, with the horizon actually in hand | match | The artboard writes 30 on two cards and 60 on the third while drawing a 60-day horizon. |
| 12 | Chart card | `#FCFAF6`, radius 12, `24px 26px`, shadow `.05` | `SECTION_CARD_FEATURE`: `padding=24px 26px; borderRadius=12px` | match | The widest of the three card paddings, on the one card the screen is about. |
| 13 | Card title | `500 17px` `#1C1A15` | `fontSize=17px; fontWeight=500; color=rgb(28,26,21)` | match | |
| 14 | Prophet footnote | 15px `#8E4718` `info`, `400 11.5px` `#4A443A`, a `#9A4E1F` link, 5px under the title | a 15px glyph in `--k-banner-btn-ink`, `text-[11.5px]` in `--k-ink-2`, the link in `--k-accent-text` | match | A footnote, not an amber banner: the projection still works, and the page is not an error page. |
| 15 | Legend | four keys on the title line, 16px apart, `400 11.5px` `#6B6353`: a 16x2 ink rule, a 16px dashed `#B4591F` rule, a 12x9 `#EFCDB2` block, a 16px dotted `#6B6353` rule | `k-chart-key`: `fontSize=11.5px; color=rgb(107,99,83); gap=6px`, the four swatches drawn as `::before` | match | Three keys, not four: the dotted baseline is only drawn when a preset has moved the line off it. |
| 16 | Chart height | 300px | `height=300px` | match | It was `h-96` (384px). |
| 17 | Chart axes | y in IBM Plex Mono 10.5 with no suffix, x in Libre Franklin 11; gridlines `#EDE7DA`; today a dashed ink rule with a label | `AXIS_LABEL_MONO` and `AXIS_LABEL_BODY`, the `today` markLine, the y labels reading `120 000` down to `0` | match | The currency came off the gridlines: eleven "zł" down the left edge say the same thing eleven times. And the grouping is the page's: ECharts' own `{value}` writes `120,000` at four figures, so the axis takes `AXIS_VALUE_SPACED`. |
| 18 | Confidence band | a smooth `#EFCDB2` wedge either side of the prediction | the same two stacked series in `CHART_BAND`, drawn from the forecaster's own daily bounds | deviation | The band is spiky rather than smooth because the seasonal-naive fallback's bounds swing day to day. The shape is the data's, not the option's. |
| 19 | Scenario annotations | a vertical `#B4591F` rule, a ringed dot and a label per scenario | the same `markLine` and `markPoint`, drawn per scenario | match | No scenarios on this ledger, so they are not in the picture. |
| 20 | Scenario chips | inside the chart card, under a 1px `#EDE7DA` rule 18px down with 18px of padding, behind a `Scenarios` eyebrow | `k-sentence-foot`: `marginTop=18px; paddingTop=16px; borderTop=1px solid rgb(237,231,218)` with the `RAIL_EYEBROW` face | deviation | 16px of padding, not 18. It is the same foot `3e` draws under its sentence, `3e`'s artboard writes `padding-top:16px` where this one writes 18, and `k-sentence-foot` is one class: it takes `3e`'s. |
| 21 | Two bottom cards | grid `1fr 1fr`, 20px apart; each `#FCFAF6`, radius 12, `22px 24px` | two `SECTION_CARD_WIDE`s `flex-1` in a `no-wrap` row, `gap-5`, each `padding=22px 24px` | match | One says what the line will be worth, the other what is already inside it. |
| 22 | Their heads | title `500 16px` and a `400 12px` `#6B6353` line under it | `CARD_TITLE_SM` over a 12px muted line | match | |
| 23 | "Next 14 days" grid | `88px 1fr 1fr 1fr`; head `600 10px/.14em` uppercase over 1px `#E2DBCC`; rows `400 12.5px` mono tabular, `9px 0`, 1px `#EDE7DA` between | `gridTemplateColumns=88px 148px 148px 148px` (the three `minmax(0,1fr)` resolved); `fontSize=10px; letterSpacing=1.4px; borderBottom=1px solid rgb(226,219,204); paddingBottom=9px`; rows `fontSize=12.5px; padding=9px 0px; borderBottom=1px solid rgb(237,231,218)` | match | `minmax(0, …)` rather than a bare `fr`: an auto-sized track takes its minimum from its content, so one long name was giving its own row different columns from every other. |
| 24 | "Planned in this period" grid | `80px 1fr 1fr 118px`; date mono 12px `#6B6353`, name and category `400 13px`, amount right in the income / expense tone | `gridTemplateColumns=80px 163px 163px 118px` — the artboard's two fixed tracks and its two equal flexible ones, resolved on a 602px card; the same four faces and tones | match | `minmax(0, …)` rather than a bare `fr`, for the reason row 23 gives. The tracks had been 72 and 104 with a 1.2:1 split, which cost the date column eight pixels and the amount fourteen for nothing. |
| 25 | Figures | IBM Plex Mono, tabular, thousands spaced, no currency inside a grid of figures | `_plain` writes `82 149.03`, the planned card `-2 400.00` and `+9 189.22`, the stat cards keep `zł` | match | The card says once what the column is in; ninety cells repeating it is ninety readings of the same fact. The planned card's figures are `TransactionService`'s signed strings and were writing `-2,400.00` until they went through `spaced_thousands` too. |
| 26 | Sample data | 92 days of history, two scenarios | 165 days, none | match | Seed data and the forecaster's own history window. |
