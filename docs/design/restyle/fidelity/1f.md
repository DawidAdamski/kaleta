---
artboard: 1f
views_hash: 8a88d619e16bc1cb
shot_at: 2026-09-19
---

# Fidelity report - 1f Dashboard, phone

Spec: [`artboards/1f.html`](../artboards/1f.html). Pictures: `.fidelity/1f/index.html` after `shoot 1f`.

Read at 390x844, seeded demo ledger. Artboard values are the inline ones in
`1f.html`; app values are `getComputedStyle` off the running page.

Two things shape this report. The plan's Scope says **the phone shell is
unchanged** in this pass - the tab bar and the drawer behind "More" are
`restyle-dashboard-mobile`'s, and the rows below that compare them are
recorded, not repaired. And `1f` is drawn inside a phone frame: a rounded
390px card with a 9:41 status bar and a home-indicator gutter, none of which
a browser page has.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Device frame | 26px-rounded card, 44px status bar, `#F3EFE7` | none - the page begins at the header | deviation | The frame is how a phone artboard is drawn, not something to build. |
| 2 | Header | 8px 20px 0 on the ground, wordmark 600/15 `#1C1A15`, `search` 22px `#4A443A`, 28px `#DE7B45` avatar, 14px apart | the wide window's 60px paper bar: `rgb(252,250,246)`, 24px side padding, wordmark 17px, a hamburger before it, `search` 24px in the accent, 28px avatar, dark toggle between | deviation | One header at both widths. What is phone-specific is what it drops: the divider and the page name are `display:none` below 768px - kept, they pushed the avatar onto a second line the 60px bar then clipped. The hamburger and the dark toggle are the app's, not the artboard's. |
| 3 | Page gutter | content at 20px from each edge | `.k-dash-page` padding 20px 16px 28px | deviation | 16px, the phone gutter `restyle-dashboard-mobile` set for every page, against the artboard's 20px on this one. |
| 4 | Bands | "Month" and "Latest" as 600/9.5/.2em uppercase `#1C1A15` over a 1px `#DCD4C2` rule, 10px above it and 14px under | `.k-band-title` 9.5px/600, letter-spacing 1.9px, uppercase, `rgb(28,26,21)`, `padding-bottom:10px` over `1px solid rgb(226,219,204)` | match | |
| 5 | How many bands | the hero, "Needs attention", Month, Budgets, Latest - no heading over the first two | four headed bands: Now, This month, Watch, Latest | deviation | `KAL-DSH-007`. The artboard leads straight into the hero; the app heads that band "Now", which is what makes the four read as one argument rather than as a wall. |
| 6 | Hero: eyebrow | 600/9.5/.2em `#6E6656`, "Safe to spend - 28 days left" | 10px/600, ls 2px, `rgb(110,102,86)`, "SAFE TO SPEND" with "12 days left this month" on its own line under the figure | deviation | `.k-eyebrow` is one size across the app (10px). The days-left line reads under the figure rather than in the eyebrow, where `days_left_label` has room to say which month it is counting. |
| 7 | Hero: figure | 400/46px/1 IBM Plex Mono, -.04em, `#1C1A15`, with a 400/17 ", 15 zl" suffix | 46px/500 IBM Plex Mono, ls -1.89px, `rgb(28,26,21)`, decimals in `--k-muted-strong` at the same size | deviation | `hero_figure` is one treatment, shared with the balance card's 54px/500 hero in `1c`: same weight, same muted decimals, and the size passed per call - 46px here. A second weight for one card is a token nobody else would reach for. |
| 8 | Hero: split bar | 9px, radius 5, `#E2DBCC` track; `#1C1A15` committed, `#8E8676` spent, `#DE7B45` free | `.k-split` 10px, radius 999px, `--k-surface-sunken`; `--k-ink` / `--k-neutral-bar` / `--k-accent-light` | deviation | The three segment colours are exact. The track is `.k-split`, one bar shared with the net-worth balance sheet (`3b`), which sets its own height and radius; reconciling the two belongs to `restyle-fidelity-screens`, which owns `3b`. |
| 9 | Hero: segment legend | "Committed 2 984 / Spent 1 286 / Free 1 942", 400/11 `#6B6353`, spread across the bar | the same three with a dot each, wrapping to two lines at 390px | deviation | Seed data - "8,896.21" is three characters longer than the artboard's sample and will not sit three abreast. |
| 10 | Needs attention | a *paper* card: eyebrow + a mono count on one line, then one 44px row per item with a 6px accent dot and a `chevron_right`, hairline between | the accent banner `1c` carries, full width, items stacked with their severity icons and the pill under them | deviation | `1f` gives this widget a second, phone-only rendering. A widget's `render` is `(session, is_dark)` - it is not told the width - so a phone variant means changing the signature every widget in the catalogue is registered with. That is a registry change, not a restyle, and it is called out in the plan's Implementation notes for the owner. |
| 11 | Month band | bare type on the ground: In/Out/Net at 500/18 mono, a 120px SVG, month letters under it | the same widgets the wide window gets, each in its 14px paper card - balance, month, cashflow, variances and the rest | deviation | One widget set, two arrangements: `bands_for_layout` groups the stored layout, it does not re-draw the widgets. The artboard draws a phone-only pair of sections in place of the cards. |
| 12 | Cards | `#FCFAF6`, radius 14, padding 16px 18px, shadow `0 1px 2px rgba(28,26,21,.05)` | `.k-dash-card` - `rgb(252,250,246)`, 14px, padding 26/28, the same shadow | deviation | The dashboard card has one padding, `1c`'s 26/28; `1f` tightens it to 16/18 for the narrower page. |
| 13 | Latest band | rows 52px tall, payee 400/13.5 `#1C1A15` over "03.07 - Zywnosc" 400/11.5 `#6B6353`, amount 500/14 mono, 1px `#E7E0D0` between | the `recent_transactions` table, five columns, in a card | deviation | Same widget, same card. `1f` re-cuts the table into two-line rows for a 390px screen; that is the phone-only rendering of row 10, for the same reason. |
| 14 | Watch band | not on the artboard | four figures as plain type, two abreast: label 11px `rgb(107,99,83)`, figure 19px/500 mono `rgb(28,26,21)` | deviation | `KAL-DSH-007`. `1f` is one screen of a scrolling page and stops before these; they are out of this plan's scope either way ("the Watch figures all stay for the phone"). |
| 15 | Tab bar | 72px tall with a 10px foot, `#FCFAF6` over 1px `#E2DBCC`; four labels 10px, the one you are on `#9A4E1F`; centre disc 52px `#1C1A15` with a `#F3EFE7` `add`, lifted 22px | 61px, `rgb(252,250,246)`, `1px solid rgb(226,219,204)`; labels 10px, active `rgb(154,78,31)`; disc 44px `rgb(180,89,31)` | deviation | Scope: "Shell, < `md`: unchanged." The bar is `restyle-dashboard-mobile`'s. Its height and the disc - 44px in the accent against `1f`'s 52px in ink - are recorded here for whichever plan next opens the phone shell. |
| 16 | Tab bar: labels | Overview, Money, Plan, Insight | Home, Ledger, Plan, More | deviation | Same scope note. "More" is the drawer, which is what carries the long tail of setup pages; the artboard's fifth slot is a page. |
| 17 | Sideways scroll | none | `scrollWidth` 390 = `innerWidth` 390 | match | `KAL-DSH-007` asserts this; it is the one thing a phone layout can get wrong invisibly. |
| 18 | Tab bar in a full-page shot | at the foot of the frame | at 839px, then the page continues under it | deviation | The bar is `position:fixed`. `full_page=True` photographs it once, where the viewport had it. |
