---
artboard: 1c
views_hash: 6c8ad07581250b90
shot_at: 2026-09-20
---

# Fidelity report - 1c Dashboard, light

Spec: [`artboards/1c.html`](../artboards/1c.html). Pictures: `.fidelity/1c/index.html` after `shoot 1c`.

Read at 1360x900, drawer expanded, seeded demo ledger. Artboard values are
the inline ones in `1c.html`; app values are `getComputedStyle` off the
running page, not a guess from the screenshot.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Header bar | 60px, `#FCFAF6`, 1px `#E2DBCC` under, 24px side padding, 20px gap | 60px, `rgb(252,250,246)`, `1px solid rgb(226,219,204)`, padding-left 24px, gap 20px | match | |
| 2 | Header contents, left | wordmark 600/17/-.01em `#1C1A15` - 1x20px `#E2DBCC` rule - page name 400/13 `#6B6353` | same, then a `chevron_left` mini toggle | deviation | The toggle is on no artboard: `1c` draws the drawer expanded and `2a` draws it mini, and one stored `sidebar_mini` is what gets you between them. The plan's Scope puts it back in the header, and the shoot script drives it by `data-drawer-mini-toggle`. |
| 3 | Header search pill | 6px 12px on `#F3EFE7`, 999px, `search` 16px `#6B6353`, "Search transactions, payees..." 400/12.5 `#6B6353` | min-height 28px, padding 0 12px, `rgb(243,239,231)`, 999px, icon 16px, label 12.5px `rgb(107,99,83)`, reading "Jump to..." | deviation | Open question 1, default taken: behind the pill is the Ctrl+K palette, which finds routes and not data. The label stays honest until a data search exists to put there. |
| 4 | Header contents, right | `dark_mode` 20px `#6B6353`; 28px `#DE7B45` disc, initials 600/11.5 `#241C13` | icon 20px `rgb(107,99,83)`; 28x28 `rgb(222,123,69)`, 11.5px/600 `rgb(36,28,19)` | match | Logout and "close database", which had buttons of their own beside the avatar, are inside the menu it drops. |
| 5 | Drawer geometry | 236px, `#F3EFE7`, 1px `#E2DBCC` right rule, padding 22px 0 24px | 236px, `rgb(243,239,231)`, `1px solid rgb(226,219,204)`, padding 22px 0 24px | match | |
| 6 | Drawer: entry you are on | `#FCFAF6`, radius 8, shadow `0 1px 2px rgba(28,26,21,.06)`, icon 19px `#9A4E1F`, label 600/13 `#1C1A15` | `rgb(252,250,246)`, radius 8px, `rgba(28,26,21,.05) 0 1px 2px`, icon 19px `rgb(154,78,31)`, 13px/600 `rgb(28,26,21)` | deviation | Everything but the shadow is exact; the shadow is `--k-card-shadow` at .05 against this one row's .06. The token is what every paper surface in the app lifts itself with, including the card this row is echoing, and the artboard varies it here by a hundredth. |
| 7 | Drawer: quiet entry | margin 0 12px, padding 7px 12px, 19px icon `#6B6353` + 12px gutter, label 400/13 `#4A443A` | min-height 35px, margin-left 12px, padding-left 12px, 19px icon in a 31px column, 13px `rgb(74,68,58)` | match | 44px below 768px, where the drawer is an overlay and a thumb is doing the tapping. |
| 8 | Drawer: group eyebrow | 600/10/.18em uppercase `#6E6656`, padding 20px 24px 6px | 10px/600, letter-spacing 1.8px, uppercase, `rgb(110,102,86)`, padding 20px 24px 6px - plus a chevron | deviation | Collapsing a group is behaviour a still picture cannot draw (`KAL-NAV-002`). The chevron is the affordance, in `--k-muted`; hovering moves the eyebrow's ink rather than painting a band behind it. |
| 9 | Drawer: foot | `v0.1.0`, 400/11 IBM Plex Mono `#6B6353`, padding 24px 24px 0; nothing above it | same, and above it an "API docs" entry under a hairline | deviation | The app has a route the artboard's index does not list. A rule of its own keeps it from reading as a stray Setup entry. |
| 10 | Icon face | Material Symbols Outlined | Material Icons Outlined | deviation | NiceGUI self-hosts Google's four Material Icons styles; Material Symbols is a different, variable font it does not ship. One rule (`.q-icon.material-icons`) moves every icon in the app off the filled face, which at 19px beside 13px type was a blob. |
| 11 | Page column | padding 36px 40px 44px, 28px gap | same; grid starts at x=276 (236 drawer + 40) | match | `.nicegui-content`'s own 1rem is zeroed under `.k-dash-page`, or the two paddings stacked to 56px. |
| 12 | Title block | eyebrow 600/10/.2em `#6E6656` + 6px; title 300/34/1, -.02em, `#1C1A15` | 10px/600, ls 2px, `rgb(110,102,86)`; 34px/300, ls -0.68px, line-height 34px, `rgb(28,26,21)` | match | 34px is this artboard's alone - every working screen sets 32px, which is what `PAGE_TITLE` carries. |
| 13 | Title eyebrow wording | "July 2026 - 3 days in" | "SEP 2026 - DAY 19" | deviation | Seed data, and the app's own phrasing for the same fact. |
| 14 | Title-row actions | two pills, gap 10px, padding 8px 14px, `#FCFAF6` on 1px `#E2DBCC`, 999px, icon 17px `#6B6353`, label 500/12.5 `#4A443A` | min-height 36px, padding 0 14px, `rgb(252,250,246)`, `1px rgb(226,219,204)`, 999px, icon 17px `rgb(107,99,83)`, 12.5px/500 `rgb(74,68,58)`, gap 10px | match | "Edit layout" and "Customize", in that order. |
| 15 | Grid | 4 columns, 20px gap; spans 2, 2, 4, 4, 2, 2, 4 | `repeat(4, 246px)`, gap 20px; `balance_card` 2, `month_card` 2, `wizard_actions` 4, `cashflow_chart` 4, `budget_variance_month` 2, `top_merchants` 2, `recent_transactions` 4 | match | One grid, and all of it drags - `KAL-DSH-008`. |
| 16 | What is in the grid | seven cards | the fourteen of `DEFAULT_WIDGETS`, the artboard's seven in its order first | deviation | The artboard draws the page's opening, not the widget catalogue. The other seven are ordinary default widgets Customize can drop. `safe_to_spend` is not among them: it is the phone's hero. |
| 17 | Card geometry | `#FCFAF6`, radius 14, padding 26/28, shadow `0 1px 2px rgba(28,26,21,.05)` | `rgb(252,250,246)`, 14px, 26px/28px, `rgba(28,26,21,.05) 0 1px 2px` | match | |
| 18 | Balance: hero figure | 500/54/1.05 IBM Plex Mono, -.035em, `#1C1A15`, decimals `#6E6656`, 12px under the eyebrow | 54px/500 IBM Plex Mono, ls -1.89px, line-height 54px, `rgb(28,26,21)`; decimals `--k-muted-strong`; 12px | match | |
| 19 | Balance: delta line | "+1 830,00 zl" 500/13 `#36684D` - "since 1 June" 400/13 `#6B6353`, 8px under | "▼ 186.80 zl (-0.26%) vs. 20 Aug 2026", 13px, trend colour, 8px under | deviation | Seed data, and `format_kpi_trend` is the one delta line every KPI in the app carries; naming the reference date is the point of it. |
| 20 | Balance: account tiles | flex:1, padding 12px 14px on `#F3EFE7`, radius 10, label 400/11 `#6B6353`, figure 500/16 mono `#1C1A15`, 10px gap, 22px under | padding 12px/14px, `rgb(243,239,231)`, radius 10px, label 11px `rgb(107,99,83)`, figure 16px/500 mono, gap 10px, 22px under | match | |
| 21 | Balance: how many tiles | three | four - three accounts and an "Other accounts (1)" tile, wrapping to a second row | deviation | Seed data. `_MAX_ACCOUNT_TILES` is three named accounts plus the collapsed rest, which is `KAL-DSH-005`. |
| 22 | Month: three figures | In / Out / Net, 500/26 mono, `#36684D` / `#A44631` / `#1C1A15`, 28px apart, 14px under the eyebrow | 26px/500 IBM Plex Mono, same three colours, gap-x 28px, 14px under | match | The labels read "In" and "Out", not "Income" and "Expense": three figures share a half-width card. |
| 23 | Month: savings pace | 8px track radius 4 on `#EDE7DA`, fill `#36684D` to the kept share, 2px x 14px tick at the target at 35% ink; labels 400/11.5 `#6B6353`, 6px above | 8px, radius 4px, `rgb(237,231,218)`; tick 2x14 `rgb(28,26,21)` at opacity .35; labels 11.5px | match | The fill is the warning colour on this ledger: it kept 11.4% against a 20% target, which is the rule the artboard's green sample is the other side of. |
| 24 | Month: footer | 1px `#EDE7DA` rule, 18px above the pair, labels 400/11, figures 500/17 mono, 24px apart | `1px solid rgb(237,231,218)`, padding-top 18px, labels 11px, figures 17px mono, gap 24px | match | |
| 25 | Needs-attention banner | span 4, `#B4591F`, radius 14, padding 18px 24px, gap 18px; `auto_awesome` 22px white; eyebrow 600/10/.2em white; message 500/15 white; pill 9px 18px on white, 600/12.5 `#8E4718` | `rgb(180,89,31)`, 14px, padding-top 18px / left 24px, gap 18px; icon 22px; eyebrow 10px/600 `rgb(252,250,246)`; message 15px/500; pill `rgb(252,250,246)`, `rgb(142,71,24)`, 12.5px/600, 9px 18px, 999px | deviation | Geometry and the pill's two colours are exact. The white-on-accent type is `--k-on-accent` (`#FCFAF6`) where the artboard writes `#fff`: dark mode needs that pair to be a token, `1d` sets it to `#241C13`, and paper is what the light half of it is everywhere else on the page. The pill needed `color=None` - Quasar's colour helpers are `!important`, and NiceGUI's default `primary` drew the accent on the accent. |
| 26 | Cashflow: card head | title 500/17 `#1C1A15`; three keys on the same line, 16px apart - 9px squares for Income and Expense, a 16x2 rule for Net; 18px under | 17px/500 `rgb(28,26,21)`; `k-legend-dot` 9x9 radius 2, `k-legend-line` 16x2, gap 16px; margin-bottom 18px | match | The legend left the chart box for the title line, which is where the artboard has it; the card also dropped a subtitle the artboard does not carry. |
| 27 | Cashflow: series | income `#36684D` and expense `#A44631` stacked either side of zero; net a 2.5px `#DE7B45` line with `#FCFAF6` discs ringed 2.5px; axis labels Feb..Jul; 240px tall | same three colours, `symbolSize` 9 on a `rgb(252,250,246)` fill with a `rgb(222,123,69)` 2.5px border; axis Apr..Sep; `h-60` | match | The accent *series* colour is `--k-accent-light`, a shade up from the accent a surface is filled with. |
| 28 | Cashflow: axes | four y labels, three gridlines and a stronger zero rule, no currency suffix | ECharts' own tick count and gridlines, no suffix | deviation | The artboard's chart is an SVG sketch; the app's is a real ECharts series that picks its own ticks from the data. |
| 29 | Budget variances | title 500/17 + subtitle 400/12; rows 16px apart; category 400/13 `#1C1A15` with the overspend in mono `#A44631`/`#8A5A12`; 6px bar radius 3 on `#EDE7DA`, full width in the same colour; detail 400/11 mono `#6B6353` | 17px/500 + 12px; gap 16px; 13px `rgb(28,26,21)`, overspend 13px mono in the same two colours; bar 6px radius 3px `rgb(237,231,218)`; detail 11px mono | match | |
| 30 | Top merchants | title + subtitle; five rows, padding 10px 0, 1px `#EDE7DA` between and none under the last; name 400/13.5 `#1C1A15`, figure 500/13.5 mono, no currency | same geometry; `py-2.5`, `k-hairline-bottom` on all but the last, 13.5px name, 13.5px/500 mono figure, `fmt_number` | deviation | The card is empty on this seed - nothing in the last 30 days carries a payee - so it reads its empty line instead of five rows. The geometry above is the code, not the picture. |
| 31 | Recent transactions: head | title 500/17 and "View all ->" 500/12.5 `#9A4E1F` on one baseline, 16px under | 17px/500 and a 12.5px/500 `rgb(154,78,31)` button with `icon-right=arrow_forward`, margin-bottom 16px | match | The card carried an eyebrow and a "Last 10 movements" line the artboard does not; both are gone. |
| 32 | Recent transactions: columns | Date, Description, Account, Category, Amount at `88px 1.6fr 1.1fr 1.1fr 130px` | the same five in the same order; Quasar sizes them from their content | deviation | `ui.table` has no column-width spec of that shape. The order, which is what the artboard is saying, is exact. |
| 33 | Recent transactions: header rule | 600/10/.16em uppercase `#6E6656` over 1px `#E2DBCC` | 10px/600, letter-spacing 1.4px, uppercase, `rgb(110,102,86)`, `1px solid rgb(226,219,204)` | deviation | .14em, not .16em: it is the `.k-table` header the ledger itself uses, and artboard `2a` sets that one at .14em. The dashboard's copy of the list is not a second table style. |
| 34 | Recent transactions: a row | 13.5px `#1C1A15` over 1px `#EDE7DA`; date mono 12px `#6B6353` as `07-03`; category a 3px 9px pill on `#EDE7DA`, 11.5px `#4A443A`; amount right, mono, 500, income/expense colours; an uncategorised row reads a bare em dash | 13.5px `rgb(28,26,21)` over `1px solid rgb(237,231,218)`; date 12px mono `rgb(107,99,83)` as `09-26`; chip `rgb(243,239,231)`, 999px, 11.5px `rgb(74,68,58)`, padding 3px 9px; amount right, mono, `amount_body_cell_slot`; em dash in `--k-muted` with no pill | match | `MM-DD` drops the year, which is the artboard's format and costs a ledger quiet enough for ten rows to cross a new year: two Decembers then read alike. The ledger itself, one click away behind "View all", carries the full date. |
| 35 | Drawer in a full-page shot | runs the height of the page | stops at 900px, then ground | deviation | The drawer is `position:fixed`, which is what a sidebar that does not scroll away means. `full_page=True` photographs it once, at the viewport. |
