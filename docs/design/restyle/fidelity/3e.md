---
artboard: 3e
views_hash: ff995ff00822f936
shot_at: 2026-09-20
---

# Fidelity report - 3e Report builder

Spec: [`artboards/3e.html`](../artboards/3e.html). Pictures: `.fidelity/3e/index.html` after `shoot 3e`.

Read at 1360x900, mini drawer, after pressing Run on the default query
(`_prepare_report_run`). Artboard values are the inline ones in `3e.html`;
app values are `getComputedStyle` off the running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Field rail | 214px, `#F3EFE7`, 1px `#E2DBCC` right rule, `26px 18px`, full height | `width=214px; padding=26px 18px; borderRight=1px solid rgb(226,219,204); backgroundColor=rgb(243,239,231)`, 757px tall | match | Its own column against the drawer, not a card on the page: `k-page--flush` takes the page's padding off and lays it on its side. |
| 2 | Rail eyebrow | `600 10px/.16em` uppercase `#6E6656`, 11px under | `fontSize=10px; fontWeight=600; letterSpacing=1.6px; color=rgb(110,102,86)` | match | Tighter tracking than a page eyebrow: it labels a list two words wide, not a screen. |
| 3 | Rail row | `7px 10px`, radius 7, `400 12.5px` `#4A443A`, 9px gap, 5px apart | `padding=7px 10px; borderRadius=7px; fontSize=12.5px; gap=9px`, 177px wide | match | Full width of the rail's interior: without it the highlight shrink-wrapped its own label. |
| 4 | Rail row, chosen | `#FCFAF6` with shadow `.05`, `500 12.5px` `#1C1A15` | `backgroundColor=rgb(252,250,246); boxShadow=rgba(28,26,21,.05) 0 1px 2px; fontWeight=500; color=rgb(28,26,21)` | match | The only paper in the rail. |
| 5 | Row glyph | `drag_indicator` 16px, `#9A4E1F` on the chosen row and `#8E8676` elsewhere | the same handle in `--k-accent-text` / `--k-muted` | match | The handle, not the field's own glyph: the rail says "this row is draggable" seven times over rather than asking anyone to tell a wallet from a bank at 16px. |
| 6 | Saved list | `400 12.5px` `#4A443A`, 8px apart, no surface | `k-rail-saved` at 12.5px in `--k-ink-2` | match | No saved reports on this ledger, so the section is not in the picture. |
| 7 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656` | `fontSize=10px; letterSpacing=2px`, reading "Unsaved report - 1 527 transactions in the ledger" | deviation | "In the ledger", not the artboard's "in scope": the count of rows the current filters admit would need the service to answer, and this plan's Scope rules service changes out. |
| 8 | Page title | `300 32px/1`, `-.02em`, reading "Reports" | `fontSize=32px; fontWeight=300` | match | The report's name moved to the eyebrow, the way every other screen names what is in hand. |
| 9 | Title-row actions | two 999px pills 9px apart: Export CSV on paper with a 1px `#E2DBCC` rule, Save report on ink with a 16px bookmark | `k-title-action` and `k-title-action--ink`, in that geometry | deviation | Run wears the ink, not Save: the artboard draws a report that has already run, and on this screen the primary action is running it. There is no Export CSV - the app has no report export, and adding one is new behaviour. |
| 10 | Sentence card | `#FCFAF6`, radius 12, `22px 24px`, shadow `.05` | `SECTION_CARD_WIDE`: `padding=22px 24px` | match | |
| 11 | The sentence | `400 15px/2` `#4A443A`, 9px gaps, wrapping | `text-[15px] leading-8` with `gap-x-1` | match | Prose, not a form: *Show Total Amount grouped by Category for Expense over This Year, top 10.* |
| 12 | A slot in it | `5px 12px` on `#F3EFE7`, 1px `#D6C6B2`, radius 8, `500` ink, a 15px `#6B6353` chevron 7px away | `padding=5px 12px; borderRadius=8px; backgroundColor=rgb(243,239,231); border=1px solid rgb(214,198,178); fontWeight=500; color=rgb(28,26,21); gap=7px` | match | It was a dashed underline; a pill is what makes the query legible as a query rather than as underlined prose. |
| 13 | Chart row | `Chart` eyebrow, five squares, a 1x22 `#E2DBCC` rule, then the filter chips; 18px over a 1px `#EDE7DA` rule with 16px of padding | `k-sentence-foot`: `marginTop=18px; paddingTop=16px; borderTop=1px solid rgb(237,231,218)` | match | |
| 14 | Chart-type square | 34x34, 1px `#D6C6B2`, radius 8, 18px glyph `#4A443A`; chosen on `#1C1A15` with a `#F3EFE7` glyph | `width=34px; height=34px; borderRadius=8px`; chosen: `backgroundColor=rgb(28,26,21); color=rgb(243,239,231)` | match | |
| 15 | Filter chips | a set one on `#F3EFE7` with a close, an unset one on 1px dashed `#CFC5AE` | `k-filter-chip` and `k-filter-chip--empty`, the same two states | match | No filter is set on this query, so only the dashed pair is in the picture. |
| 16 | Result card | `#FCFAF6`, radius 12, `24px 26px`, shadow `.05` | `SECTION_CARD_FEATURE`: `padding=24px 26px` | match | |
| 17 | Its title line | `500 17px` `#1C1A15` and `400 12.5px` mono `#6B6353` "total 44 218,60 zł" on the same baseline, 20px under | `CARD_TITLE`: `fontSize=17px; fontWeight=500`; `k-result-total`: `fontFamily="IBM Plex Mono"; fontSize=12.5px; color=rgb(107,99,83)` | deviation | The total carries no `zł`: this query can be a count or an average, and a currency on either would be wrong. |
| 18 | Bar row | grid `196px 1fr 108px 62px`, 14px gaps, 13px apart | `gridTemplateColumns=196px 562px 108px 62px; gap=14px` | match | |
| 19 | The bar | 22px track radius 3 on `#EDE7DA`, filled to the value | `height=22px; borderRadius=3px; backgroundColor=rgb(237,231,218)`, fill the same height and radius | match | Rows of HTML, not a canvas: ten labelled bars inside an ECharts frame could not be selected, searched or read aloud. |
| 20 | Bar colours | six greens darkest first: `#36684D` down to `#A8C8B6` | `--k-ramp-1` .. `--k-ramp-6`, the same six in light and their dark counterparts | match | A ranking you can read without a legend. |
| 21 | Figures on a row | name `400 13px` `#1C1A15`; value `500 13px` mono tabular right; share `400 12px` mono `#6B6353` right | the same three faces and alignments | match | |
| 22 | Sample data | Polish expense categories over a year | the seeded ledger's own | match | |
