---
artboard: 3f
views_hash: 22f79a2e84b1316d
shot_at: 2026-09-21
---

# Fidelity report - 3f Login, desktop and phone

Spec: [`artboards/3f.html`](../artboards/3f.html). Pictures: `.fidelity/3f/index.html` after `shoot 3f`.

Read at 1360x900 and at 390x844, signed out, after one failed attempt
(`_prepare_login_error`). Artboard values are the inline ones in `3f.html`;
app values are `getComputedStyle` off the running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Split | a form column on `#F3EFE7` and a `300px` ink panel; no card floating in a viewport | the same split, the panel `width=503px` - 37% of 1360, capped at 560 | match | The artboard's card is 820px wide, so its 300px panel is 36.6% of it; 37% is that ratio at any width. |
| 2 | Form column | `padding:64px 60px`, wordmark at the top, form in the middle, version at the foot | `p-8 md:py-16 md:px-[60px]`, two `flex-1 min-h-[36px]` spacers | match | The column is spaced with two equal flexible gaps rather than centring the lot, which is what puts the version on the foot. |
| 3 | Wordmark | 26px `#B4591F` glyph and `600 21px` `#1C1A15` "Kaleta", 11px apart | `ui.icon(size="26px")` in `--k-accent-text` and `text-[21px] font-semibold tracking-tight`, `gap-[11px]` | match | |
| 4 | Title | `300 30px/1.2`, `-.02em`, `#1C1A15` | `fontSize=30px; fontWeight=300; letterSpacing=-0.6px; color=rgb(28,26,21)` | match | |
| 5 | Subtitle | `400 13.5px/1.6` `#4A443A`, 10px under, 340px wide | `fontSize=13.5px; lineHeight=21.6px; color=rgb(74,68,58); maxWidth=340px` | match | |
| 6 | Field label | `600 10px/.16em` uppercase `#6E6656`, 7px above the field | `fontSize=10px; fontWeight=600; letterSpacing=1.6px; color=rgb(110,102,86); textTransform=uppercase` | match | Above the field, not floating inside it: Quasar's own label stays as the accessible name, and the eyebrow is type on the page. |
| 7 | Field | `12px 14px` on `#FCFAF6`, 1px `#D6C6B2`, radius 9, `400 14px` — about 41px tall | `backgroundColor=rgb(252,250,246); border=1px solid rgb(214,198,178); borderRadius=9px; padding=0 14px; minHeight=48px`, measuring 56px with Quasar's own inner metrics | deviation | The ground, the outline, the radius and the type match; the height does not. A 48px floor rather than 12+12+17: on a phone this form is the whole screen, and a 40px target in the middle of it is one the thumb has to aim at. Quasar's control adds its own 8px on top of the floor, which is where 56 comes from. |
| 8 | Field, focused | 1.5px `#1C1A15` | `border:1.5px solid var(--k-ink)` on the focused control, computing to `borderWidth=1px; borderColor=rgb(28,26,21)` | match | Chrome rounds a border to whole device pixels, so 1.5px comes back as 1px at this ratio — off the artboard's own markup as much as off the app's. The padding drops to 13.5px with it, so the box does not grow by the half-pixel it gained. |
| 9 | Password field | the same box with a 19px `#6B6353` eye at the right, the dots in mono at `.12em` | the same, Quasar's `password_toggle_button` | match | |
| 10 | Error strip | `11px 13px` on `#F6E2DC`, radius 8, 17px `#8A3826` glyph, `400 12.5px` `#8A3826` | `minHeight=42px; backgroundColor=rgb(246,226,220); color=rgb(138,56,38); borderRadius=8px; padding=11px 13px; fontSize=12.5px` | match | It keeps its line whether or not it has anything to say: showing and hiding it moved the button down under the pointer at the moment of the second attempt, which is how a retry became a misclick. |
| 11 | Error wording | "Wrong username or password. 3 attempts left." | "Invalid username or password." | deviation | The count exists - `LoginRateLimiter` keeps it - but nothing exposes it, and `src/kaleta/auth/` is not in this plan's Touchpoints. |
| 12 | Submit | `13px 0` on `#1C1A15`, radius 9, centred `600 13.5px` `#F3EFE7`, full width — about 43px tall | `backgroundColor=rgb(28,26,21); color=rgb(243,239,231); borderRadius=9px; fontSize=13.5px; fontWeight=600`, measuring 340x49 | deviation | Ink, radius, type and width match; the height does not, for the same reason as row 7 — the page's one action sits on the same 48px floor as the fields above it. No icon: on a page with one action there is no mark that says which one it is, and the accent belongs to the wordmark. |
| 13 | Foot | `400 11.5px` IBM Plex Mono `#6B6353`, `v0.1.0 - AGPL-3.0` | `fontSize=11.5px; fontFamily="IBM Plex Mono"; color=rgb(107,99,83)` | match | The licence is spelled out rather than read from the metadata: `pyproject` carries `AGPL-3.0-or-later`, which is not what a login page should say. |
| 14 | Panel | `#1C1A15`, `64px 40px`, bottom-aligned | `backgroundColor=rgb(28,26,21); padding=64px 40px; justifyContent=flex-end` | match | The copy and the counts sit on the panel's foot, not in the middle of an empty one. |
| 15 | Panel copy | `400 15px/1.7` `#F3EFE7` | `text-[15px] leading-[1.7]` in the panel's paper ink | match | |
| 16 | Panel rule | 1px `#3A362C`, 26px above and 22px of padding | `k-auth-rule`: `borderTop=1px solid rgb(58,54,44); marginTop=26px; paddingTop=22px` | match | A hairline on ink rather than on paper - `--k-ink-rule`. |
| 17 | Panel counts | `400 22px` mono `#F3EFE7` over `400 11px` `#B0A692`, 26px apart, 3px between | `fontSize=22px; fontFamily="IBM Plex Mono"; color=rgb(243,239,231)` over `--k-on-ink-muted`, `gap-[26px]`, `gap-[3px]` | match | |
| 18 | Third count | "3 banks" | "72 months of history" | deviation | `AuthStatsService` counts transactions, accounts and months; a fourth count is a service change, which this plan's Scope rules out. |
| 19 | Phone, 390x844 | below `md` the panel is gone and the form has the screen | the same: `.k-auth-panel` is `display:none` under 768px and `flex` above it; the column takes `p-8` | match | Read off `app-390.png`. |
| 20 | Numbers | IBM Plex Mono in the panel and in the foot | `k-mono` in both | match | |
| 21 | Sample data | 4 218 transactions, 7 accounts | 1 527 transactions, 4 accounts, 72 months - the seeded ledger's own | match | |
