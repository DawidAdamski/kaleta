---
artboard: 2d
views_hash: 64d6ff4e1ed05d5d
shot_at: 2026-09-21
---

# Fidelity report - 2d Import, step 3

Spec: [`artboards/2d.html`](../artboards/2d.html). Pictures: `.fidelity/2d/index.html` after `shoot 2d`.

Read at 1360x900, mini drawer, after uploading `test_import.csv` and
stepping to Mapping (`_prepare_import_mapping`). Artboard values are the
inline ones in `2d.html`; app values are `getComputedStyle` off the running
page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656`: the file's name and its row count | the same line, `data-page-eyebrow`, reading `test_import.csv - 3 rows` | match | |
| 2 | Page title | `300 32px/1`, `-.02em` | `fontSize=32px; fontWeight=300` | match | "Import", not "Import Transactions": the drawer and the header already say which screen this is. |
| 3 | Stepper | six nodes on one hairline; done nodes 22px ink discs with a tick, the current one a 22px `#B4591F` disc carrying its number, the rest 1.5px `#C9BFA8` rings | the same three node states (`k-steps`), the rule running behind them | match | |
| 4 | Which node is current | node 3, Mapping | node 3 is ticked and node 4 carries the accent | deviation | The app auto-detects the three fields it can, so by the time this screen is drawn the mapping is complete and the work has moved to Settings. Marking Mapping as still-to-do would be the picture disagreeing with the page. |
| 5 | Two cards | grid `1.25fr 1fr`, 22px apart, `align-items:start` | `gridTemplateColumns=667.766px 534.234px; gap=22px` - 1.25fr and 1fr of 1224 | match | Two cards, not two columns of a card: `k-mapping-grid`. |
| 6 | Card geometry | `#FCFAF6`, radius 12, `22px 24px`, shadow `0 1px 2px rgba(28,26,21,.05)` | `SECTION_CARD_WIDE`: `padding=22px 24px; borderRadius=12px; backgroundColor=rgb(252,250,246)` | match | The artboards draw three card paddings; this is the middle one, and it is `2d`, `3a`, `3b`, `3c` and `3e` that ask for it. |
| 7 | Card title | `500 16px` `#1C1A15`; a `400 11.5px` mono `#6B6353` line under it with `5px 0 16px` | `CARD_TITLE_SM` over a muted line | match | |
| 8 | Sample table | 1px `#E2DBCC` box, radius 8, overflow hidden; head `600 9.5px/.06em` mono uppercase on `#F6F1E7`; rows `400 11.5px` mono, `8px 12px`, 1px `#EDE7DA` between | the same box, band and two faces | match | The head names each column by its index and its header text, which is what the pickers opposite refer to. |
| 9 | Sample columns | grid `96px 1fr 92px 88px` | the same four tracks | match | |
| 10 | Unparseable-rows note | `11px 14px` on `#F4E9DC`, radius 8, 17px `#8E4718` glyph, `400 12.5px/1.5` `#4A443A` | not drawn in this shot; `k-warning-strip` is `px-[14px] py-[11px] rounded-lg` on `--k-surface-notice` (`#F4E9DC`) | deviation | Sample data: `test_import.csv` parses cleanly, so there is nothing to warn about. The note exists and is drawn when a row cannot be read - `test_parse_failures_are_named_on_the_mapping_step` reads it, under the sample rows it numbers. It used to carry a 2px accent left edge the artboard does not draw. |
| 11 | Mapping row | 12px apart; label `400 12.5px` `#4A443A` in a 104px column | `k-field-row`: `gap=12px; alignItems=center`; `k-field-label`: `width=104px; fontSize=12.5px; color=rgb(74,68,58)` | match | |
| 12 | Picker | `8px 12px` on `#F3EFE7`, 1px `#D6C6B2`, radius 8, `400 12.5px` mono `#1C1A15` | `minHeight=36px; borderRadius=8px; backgroundColor=rgb(243,239,231); border=1px solid rgb(214,198,178); padding=0 12px` | match | `#D6C6B2` is `--k-field-border`, a shade lighter than the chip outline: the artboards draw every input on it and keep `#C9BFA8` for controls that are buttons. |
| 13 | Unmapped picker | the same box reading "Not mapped" | paper rather than sunken sand (`k-field-select--unset`), reading "- not mapped -" | deviation | The mapped rows are the ones that carry weight; an empty picker that looked the same as a filled one made the six unmapped fields as loud as the three that matter. |
| 14 | AUTO mark | `AUTO` inside the box, quiet, left of the chevron | `k-auto-badge`: `9.5px/600/.06em` uppercase in `--k-muted-strong`, in the field's append slot | match | It was a green outlined pill sitting on the field's own border; `align-self:center` and no outline put it inside the box where the artboard has it. |
| 15 | Fields offered | Date, Amount, Description, Payee, Notes, Debit / Credit | the same, with Counterparty, Debit and Credit as three rows rather than one | deviation | The importer maps a debit column and a credit column separately - a two-column statement is the case they exist for - and a single "Debit / Credit" picker could not say which is which. |
| 16 | Formats row | eyebrow `600 10px/.16em`, then three pills reading the detected format | the same eyebrow and three pills, reading `Auto` | deviation | Sample data: the file's format was detected, and `Auto` is what the control holds until someone overrides it. |
| 17 | Negative-amounts checkbox | ink tick, `400 12.5px` label | the same, Quasar's checkbox with the ink tick | match | |
| 18 | Card footer | Back and "Continue to settings" inside the mapping card | one footer under both cards, at the foot of the page | deviation | The footer serves all six steps of the wizard; moving it inside the mapping card would mean six copies of it, and five of the steps have no card to put it in. |
| 19 | Numbers | IBM Plex Mono throughout the sample and the pickers | `k-mono` on both | match | |
| 20 | Colour | ink, muted, `#B4591F` on the current step, `#8E4718` on the warning glyph | `--k-ink`, `--k-muted`, `--k-accent`, `--k-banner-btn-ink` | match | |
