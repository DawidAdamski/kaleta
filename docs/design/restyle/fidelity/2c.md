---
artboard: 2c
views_hash: 263e3fde0e177f85
shot_at: 2026-09-21
---

# Fidelity report - 2c Budget Plan

Spec: [`artboards/2c.html`](../artboards/2c.html). Pictures: `.fidelity/2c/index.html` after `shoot 2c`.

Read at 1360x900, mini drawer, the seeded plan year. Artboard values are
the inline ones in `2c.html`; app values are `getComputedStyle` off the
running page, or the inline widths in `budget_plan/constants.py` where the
grid sets them itself.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656`, 6px above | `fontSize=10px; letterSpacing=2px; color=rgb(110,102,86)` | match | The help line is the eyebrow: it says what the grid under it responds to. The longer explanation moved to its tooltip, because the full sentence pushed the year pills onto a second line. |
| 2 | Page title | `300 32px/1`, `-.02em`, `#1C1A15` | `fontSize=32px; fontWeight=300` | match | |
| 3 | "Years" label | `400 11.5px` `#6E6656` | `fontSize=11.5px; fontWeight=400; color=rgb(110,102,86)` | match | |
| 4 | Year chip, off | `6px 13px`, 1px `#CFC5AE`, 999px, `400 12px` IBM Plex Mono `#6B6353` | `minHeight=28px; padding=0 13px; borderRadius=999px; fontFamily="IBM Plex Mono"; fontSize=12px; color=rgb(107,99,83)` | match | Mono because a year is a figure. 28px against the artboard's 6+6+15: a Quasar button sets its own height. |
| 5 | Year chip, on | `#1C1A15`, `500 12px` mono `#F3EFE7` | `backgroundColor=rgb(28,26,21); color=rgb(243,239,231)` | match | |
| 6 | Chip row | 6px apart, right of the title, on one line | `gap:6px` with `no-wrap`, the title column `shrink` | match | |
| 7 | "Copy into" select | `6px 12px` on `#FCFAF6`, 1px `#E2DBCC`, radius 8, `400 12.5px`, 15px chevron | `minHeight=32px; borderRadius=8px; backgroundColor=rgb(252,250,246); border=1px solid rgb(226,219,204)` | match | `k-select-pill--square`: a select drawn as a box, not as a field with an underline. |
| 8 | Copy button | `7px 14px` on `#1C1A15`, radius 8, `600 12px` `#F3EFE7` | `minHeight=30px; padding=0 14px; borderRadius=8px; backgroundColor=rgb(28,26,21); color=rgb(243,239,231); fontSize=12px; fontWeight=600` | match | Square corners, not the title row's pill: this is a control inside the screen, not the screen's one action. Its label is "Copy from previous month", which says which month it copies; the artboard's "Copy forward" does not. |
| 9 | Grid columns | `186px 62px repeat(12,minmax(0,1fr)) 96px` | flex tracks: category `0 0 175px`, month `0 0 82px`, each month `1 1 55px` with a 52px floor, year total `0 0 90px`, and a 76px actions column; the block has a `min-width:1020px` | deviation | Two differences, both deliberate. The grid is flex, not CSS grid, because it scrolls sideways under 1020px and twelve month columns cannot reflow into one; and there is a thirteenth column, for the reason row 10 gives. |
| 10 | Per-row actions | none in the grid: the designer's note moves `event_note` / `delete_sweep` "into a right-click menu on the row - deliberate, to buy back the two columns of width the grid needs" | the right-click menu the note asks for (`ui.context_menu` on the row pair), plus a 76px column ending each row with a `more_horiz` button that opens the same menu | deviation | The menu is where the note puts it, and the two always-on buttons are gone. The column is the difference: a right-click is not a gesture on a touch screen and not a key on a keyboard, so the menu would be unreachable for both without a control that opens it. One 76px column against the artboard's two. |
| 11 | Grid header | `600 9.5px/.1em` uppercase `#6E6656`, `10px 0`, 1px `#C9BFA8` under | `fontSize=10px; letterSpacing=2px; fontWeight=600; textTransform=uppercase; color=rgb(110,102,86); padding=9px 12px`; `borderBottom=1px solid rgb(201,191,168)` | deviation | The rule and the ink match (`--k-border-strong` `#C9BFA8`, `--k-muted-strong` `#6E6656` — it had been a shade darker in `--k-muted`). The size does not: the header wears the app's one eyebrow token, 10px at `.2em`, where the artboard writes 9.5px at `.1em`. A second eyebrow size for one table header is a token the rest of the app would never use. |
| 12 | Month cell, the month being edited | centred IBM Plex Mono on `#F0E5D4`, radius 4, `2px 0` | `backgroundColor=rgb(240,229,212); borderRadius=4px; padding=9px 4px; textAlign=center; fontFamily="IBM Plex Mono"` | deviation | The tint, the radius and the face match. The padding is the row's 9px rather than the artboard's 2px: the tinted cell is one of thirteen in a row and cannot be shorter than the twelve beside it. Hover outlines rather than fills, so it can sit on top of that tint. |
| 13 | Plan row | `400 12.5px` `#1C1A15`, `9px 0`; the category name 500 | cells `fontSize=12.5px; padding=9px 4px`; the name `fontSize=12.5px; fontWeight=500; color=rgb(28,26,21); padding=9px 12px`; the row `borderBottom=1px solid rgb(237,231,218)` | match | It was `text-sm` (14px) at `4px`, a size and a half too big for a table thirteen columns wide. |
| 14 | Actuals row under it | `400 10.5px` IBM Plex Mono `#6B6353`, `0 0 9px`, the label italic; over-plan `#A44631` | label `fontSize=10.5px; color=rgb(107,99,83); padding=0 12px`; figures `fontSize=10.5px; color=rgb(107,99,83); fontFamily="IBM Plex Mono"`, over-plan in `--k-expense` | deviation | Size, face and tone match; the label is upright where the artboard italicises it. One italic word in a grid of mono figures is a third face on a row that already carries two. |
| 15 | Child rows | `↳` and the name indented | the same arrow and indent | match | |
| 16 | Total rows | Planned `600 12.5px` `#1C1A15` over a 1px `#C9BFA8` rule; Actual under it | `borderTop=1px solid rgb(201,191,168)`; the label `fontSize=12.5px; fontWeight=500; color=rgb(28,26,21); padding=9px 12px` | deviation | The rule, the size and the ink match; the weight is 500 where the artboard writes 600. 500 is the app's one weight for a figure that closes a table (`2b`'s Total row, `3b`'s sheet), and a heavier one here would make this table's foot the loudest line on a screen of figures. |
| 17 | Surfaces | no card: the grid sits on the ground between two rules | no `k-surface` anywhere on the page; the grid's own `.k-plan-grid` is `background:transparent`, between the header's `#C9BFA8` rule and the total's | match | |
| 18 | Numbers | IBM Plex Mono, tabular, thousands spaced | `k-mono` with `font-variant-numeric:tabular-nums`, writing `3 234` | match | Through `spaced_thousands`, which every restyled screen now writes its grouping with rather than a `.replace` of its own. |
| 19 | Sample data | six Polish categories, July the edited month | seventeen categories and their children, September the edited month | match | Seed data and today's date. |
| 20 | Colour | ink `#1C1A15`, muted `#6B6353`, accent `#9A4E1F` on the recurring-amount cell | `--k-ink` = `#1C1A15`, `--k-muted` = `#6B6353`, and the `~` cell computing `color=rgb(154,78,31)` (`--k-accent-text`, `#9A4E1F`) | match | |
