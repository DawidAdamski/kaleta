---
artboard: 2b
views_hash: 1ea03a3937a26f17
shot_at: 2026-09-21
---

# Fidelity report - 2b Budgets - Realization

Spec: [`artboards/2b.html`](../artboards/2b.html). Pictures: `.fidelity/2b/index.html` after `shoot 2b`.

Read at 1360x900, mini drawer, Realization tab (`_prepare_realization_tab`)
on the seeded month. Artboard values are the inline ones in `2b.html`; app
values are `getComputedStyle` off the running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Page column | padding 32px 36px 40px, 22px gap | `padding=32px 36px 40px; gap=22px` | match | |
| 2 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656` | `fontSize=10px; letterSpacing=2px; color=rgb(110,102,86)` | match | Reads "September 2026 - 64% of the month elapsed": the same percentage every pace bar's tick stands at, said once instead of forty times. |
| 3 | Page title | `300 32px/1`, `-.02em` | `fontSize=32px; fontWeight=300; letterSpacing=-0.64px` | match | |
| 4 | Title-row action | `9px 16px` ink pill, 999px, `600 12.5px` `#F3EFE7` | `k-title-action--ink`, `minHeight=36px; borderRadius=999px` | match | "Edit budgets". |
| 5 | Tab row | one rule `1px #DCD4C2` under both tabs and the screen's controls, 26px apart | `borderBottom=1px solid rgb(220,212,194)`, `k-tab-row` | match | The controls that steer the tab you are on share that line; there is no second toolbar under it. |
| 6 | Chosen tab | `600 13.5px` `#1C1A15` over a 2px `#1C1A15` underline, `-1px` margin | `fontSize=13.5px; fontWeight=600; color=rgb(28,26,21)` with the ink indicator | match | |
| 7 | Month / year pickers | `6px 12px` on `#FCFAF6`, 1px `#E2DBCC`, radius 8, 12.5px; the year in mono | `k-select-pill--square`: `minHeight=32px; borderRadius=8px; backgroundColor=rgb(252,250,246); border=1px solid rgb(226,219,204)` | match | |
| 8 | Flat / By parent | `#F3EFE7` radius 8 with 2px padding; chosen `5px 12px` ink radius 6 | `k-segment`, chosen button `26px` tall, radius 6 | match | |
| 9 | Four stat cards | `flex:1` `#FCFAF6`, radius 12, `20px 22px`, shadow `.05`, 20px apart | `backgroundColor=rgb(252,250,246); borderRadius=12px; padding=20px 22px; boxShadow=rgba(28,26,21,.05) 0 1px 2px`, gap 20px | match | Planned, Actual, Remaining, Used - the same four the Total row closes the table with, so the month is answered before forty categories are read. |
| 10 | Stat figure | `500 28px` IBM Plex Mono, tabular, 6px under the eyebrow; green / red / ink by sign | `fontFamily="IBM Plex Mono"; fontSize=28px; fontWeight=500; marginTop=6px; color=rgb(28,26,21)` | match | |
| 11 | Table card | `#FCFAF6`, radius 12, `padding:6px 0`, shadow `.05` | `backgroundColor=rgb(252,250,246); borderRadius=12px; padding=6px 0px` | match | |
| 12 | Header row | grid `1.7fr 118px 118px 118px 78px 200px`, `600 10px/.14em` uppercase `#6E6656`, `12px 22px`, 1px `#E2DBCC` under | `gridTemplateColumns=548px 118px 118px 118px 78px 200px` (1.7fr resolved); `fontSize=10px; letterSpacing=1.4px; color=rgb(110,102,86); paddingBottom=12px; borderBottom=1px solid rgb(226,219,204)` | match | The last column's head says what the tick on every bar under it means - the one place on the screen that can say it once. |
| 13 | Category row | the same grid, `400 13.5px` `#1C1A15`, `13px 22px`, 1px `#EDE7DA` under | `gridTemplateColumns=548px 118px 118px 118px 78px 200px; padding=13px 22px; fontSize=13.5px; borderBottom=1px solid rgb(237,231,218)` | match | |
| 14 | Figures in a row | right-aligned IBM Plex Mono, tabular, thousands spaced | `k-mono` right-aligned in `--k-ink-2` / ink 500 / `--k-expense`, writing `2 400.00` and `5 673.69` | match | Through `spaced_thousands`, as every restyled screen does; this card was writing `2,400.00` beside `2c`'s `2 400`. |
| 15 | Pace bar | 7px track radius 4 on `#EDE7DA`, filled to what was spent | `height=7px; borderRadius=4px; backgroundColor=rgb(237,231,218)`, 178px wide | match | Plus a tick where the month stands, which is what turns "Warning" from a word into a comparison. The status word moved to the bar's tooltip. |
| 16 | Note under a bar | `400 10.5px` `#6E6656`, italic where it is a hint | `k-realization-note`, wrapping rather than truncating | deviation | "Paid in full on 01.09 - as planned" does not fit one 178px line, and half an explanation explains nothing. |
| 17 | Total row | `600 13.5px` `#1C1A15`, `14px 22px`, under the last category row's 1px `#E2DBCC` | `padding=14px 22px; fontWeight=600; fontSize=13.5px; borderTop=1px solid rgb(226,219,204)` | match | The artboard draws the closing rule as the last row's bottom border and the app as the Total row's top border, which is the same line in the same colour (`--k-border`, `#E2DBCC`) - a stronger one than the `#EDE7DA` between the category rows, so the table is closed by the line it was opened with. |
| 18 | Grouped view | a parent's name on a rule with nothing behind it | `k-realization-group`: a folder glyph and the parent's name in the eyebrow face, no figures | match | A parent is not a row you can read figures off. |
| 19 | Sample data | one month of Polish categories, one of them over | the seeded month, `_PCT_TONE` keyed on the row's own status so the percentage and the bar never disagree | match | |
| 20 | Colour | income `#36684D`, expense `#A44631`, warning `#8A5A12` | `--k-income`, `--k-expense`, `--k-warning` | match | |
