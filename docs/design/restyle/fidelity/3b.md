---
artboard: 3b
views_hash: f8f8e72d576726ac
shot_at: 2026-09-21
---

# Fidelity report - 3b Net Worth

Spec: [`artboards/3b.html`](../artboards/3b.html). Pictures: `.fidelity/3b/index.html` after `shoot 3b`.

Read at 1360x900, mini drawer, the seeded balance sheet. Artboard values
are the inline ones in `3b.html`; app values are `getComputedStyle` off the
running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Title eyebrow | `600 10px/.22em` uppercase `#6E6656` | `fontSize=10px; letterSpacing=2px` | deviation | `.2em`, not `.22em`: `PAGE_EYEBROW` is one token and every other screen's artboard sets it at `.2em`. |
| 2 | Hero figure | `400 60px/1` IBM Plex Mono, tabular, `-.04em`, `#1C1A15`, on the ground and not on a card; `128 940` | `fontSize=60px; fontWeight=400; fontFamily="IBM Plex Mono"; letterSpacing=-2.4px; color=rgb(28,26,21)`, reading `770 654` | match | Spaced through `spaced_thousands`, like the deltas, the legend, both sheets and the physical-assets card — every one of them was writing `770,654` under an artboard that writes a space. |
| 3 | Hero decimals | `400 22px` mono `#6E6656`, on the same baseline | `k-nw-decimals` at 22px in `--k-muted-strong`, `align-items:baseline` | match | |
| 4 | Delta line | two pairs 28px apart, 16px under; label `400 12px` `#6B6353`, figure `500 14px` mono `#36684D` | `k-delta-label` / `k-delta-figure` at those sizes and tones | match | "vs 30 days ago" and "vs 1 January": a pill would have made two facts look like one badge. |
| 5 | Split bar | 12px tall, radius 6, on `#E2DBCC`, max 620px, 26px under the deltas; three segments meeting with no gap | `height=12px; borderRadius=6px; backgroundColor=rgb(226,219,204)`, 620px wide | match | The shape of the sheet, which a net figure cannot show: 10 000 owned outright and 200 000 against 190 000 owed are the same number and not the same position. |
| 6 | Split legend | 9x9 swatches radius 2, 26px apart, `400 12px` `#4A443A`, the figure in mono ink | `width=9px; height=9px; borderRadius=2px` | match | No percentages: the bar is the proportion. |
| 7 | Physical-assets card | 420px, `#FCFAF6`, radius 12, `22px 24px`, shadow `.05` | `SECTION_CARD_WIDE` at `w-[420px]`: `width=420px; padding=22px 24px` | match | |
| 8 | Its title row | `500 15px` `#1C1A15` and the total in `500 13px` mono, 14px under | `CARD_TITLE_SM` and a mono total | match | |
| 9 | Asset row | grid `1fr 96px 108px 30px`, `400 13px`, `9px 0`, 1px `#EDE7DA` under; type `11.5px` `#6B6353`; amount right-aligned mono | `k-asset-row`: `padding=9px 0px; borderBottom=1px solid rgb(237,231,218); fontSize=13px` | match | |
| 10 | Asset row actions | one 16px pencil in a 30px column | a pencil and a bin in a 56px column | deviation | Deleting an asset has to be reachable; a 30px column put the two buttons on top of each other and wrapped the row. |
| 11 | "Add asset" | `500 12.5px` `#9A4E1F` with a 16px `+`, 14px over a 1px `#EDE7DA` rule | `k-asset-add` in `--k-accent-text` over the same rule | match | |
| 12 | Chart card | `#FCFAF6`, radius 12, `24px 26px`, shadow `.05` | `SECTION_CARD_FEATURE`: `padding=24px 26px` | match | |
| 13 | Chart head | title `500 17px`, a `400 12px` `#6B6353` line under it, and the two keys on the title line at the right | `CARD_TITLE` over a muted line, `k-legend-dot` keys at the right | match | The legend left the frame for the title line, which is where the artboard has it. |
| 14 | Chart | two stacked bands, assets `#36684D` under liabilities `#A44631`, the top edge the two combined; y `0 / 45k / 90k / 135k / 180k`; x six labels | the same two stacked series and tones; the axes pick their own ticks from a ledger an order of magnitude larger, so the y labels read `0k` to `1,200k` and thirteen months are named | deviation | Axis ticks are ECharts' own, from the data. The artboard's SVG draws five by hand. Their grouping is not ECharts' own, though: `AXIS_VALUE_SPACED` makes them read `1 200k`, not `1,200k`. |
| 15 | Chart axis faces | y in IBM Plex Mono 10.5, x in Libre Franklin 11.5 | `AXIS_LABEL_MONO` and `AXIS_LABEL_BODY` | match | |
| 16 | End-of-series annotations | `+21 278 liabilities` and `150 218 assets` written at the right end of each band | the two end values, `130.7k` and `901.4k`, in the series' own colours | deviation | The figure, not the sentence: the legend on the title line already says which band is which, and the word would be said twice. |
| 17 | Assets / Liabilities cards | side by side; title `500 15px` with the total right, `#36684D` and `#A44631` | the same pair, `CARD_TITLE_SM` and a mono total in the income / expense tone | match | |
| 18 | Sheet header | `600 10px/.14em`-class eyebrow over 1px `#E2DBCC`, `padding-bottom:9px` | `k-sheet-head`: `fontSize=10px; letterSpacing=1.4px; color=rgb(110,102,86); borderBottom=1px solid rgb(226,219,204); paddingBottom=9px` | match | |
| 19 | Sheet row | grid `1fr 96px 108px 30px`, `400 13px`, `9px 0`, 1px `#EDE7DA` under; an owed balance negative and in `#A44631` | `gridTemplateColumns=167px 92px 167px 132px; fontSize=13px; padding=9px 0px; borderBottom=1px solid rgb(237,231,218)`, writing `264 394.00 PLN`; liabilities drawn `-abs(balance)` in `--k-expense` | deviation | Four tracks, not the artboard's: this table has an institution column the physical-assets one does not, and the balance column has to hold `12 418,40 ≈ 2 870,10 PLN` on a foreign-currency account. |
| 20 | Personal-loans note | `#F3EFE7` aside inside the liabilities card, 16px glyph, `400 12px` `#4A443A`, a `#9A4E1F` link | `k-card-note`: `backgroundColor=rgb(243,239,231); borderRadius=8px; padding=11px 13px; fontSize=12px; color=rgb(74,68,58)` | match | Money lent to a friend is an asset by any honest reckoning; the app tracks it on its own page, so the card says where it lives rather than quietly under-counting. |
| 21 | Currency | the hero says `zł`; the rows and totals carry no suffix | every figure names its currency (`PLN`) | deviation | The ledger is multi-currency: a row can be in EUR beside a row in PLN, and an unsuffixed column would be two different things in one line. |
| 22 | The decimal mark | `,00` — the artboards are Polish mock-ups and write the Polish comma | `.16`, `.00`, `.35` — a dot, in both locales | deviation | The thousands are the artboard's space now, everywhere on this screen. The decimal mark is not: `f"{amount:,.2f}"` is what the whole app writes, in Polish as in English (`tests/unit/views/test_realization_note.py::TestPolishTemplates` pins it), and moving it is a locale change across every screen, service and export rather than a restyle of ten. Recorded here, once, on the screen that draws a decimal at 22px. |
