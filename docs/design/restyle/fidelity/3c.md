---
artboard: 3c
views_hash: c1f508a835550fba
shot_at: 2026-09-20
---

# Fidelity report - 3c Payment Calendar

Spec: [`artboards/3c.html`](../artboards/3c.html). Pictures: `.fidelity/3c/index.html` after `shoot 3c`.

Read at 1360x900, mini drawer, the seeded month with two overdue items.
Artboard values are the inline ones in `3c.html`; app values are
`getComputedStyle` off the running page.

| # | Element | Artboard | App | Status | Note |
|---|---|---|---|---|---|
| 1 | Title eyebrow | `600 10px/.2em` uppercase `#6E6656`: what is on the grid | `fontSize=10px; letterSpacing=2px; color=rgb(110,102,86)`, reading "Planned transactions and subscription charges" | match | |
| 2 | Page title | the month, `300 32px/1`, `-.02em`, with a 22px chevron either side 14px away | `fontSize=32px; fontWeight=300`, `chevron_left` / `chevron_right` at 22px in ink, `gap:3.5` | match | The screen's name is in the drawer and the header; the page's big type goes to the thing the arrows change. |
| 3 | Title-row actions | Today and List view as paper pills on 1px `#E2DBCC`, Post all due on ink; 9px apart | `k-title-action`: `minHeight=36px; padding=0 14px; borderRadius=999px; backgroundColor=rgb(252,250,246); border=1px solid rgb(226,219,204); fontSize=12.5px`, and `k-title-action--ink` on the third | match | |
| 4 | Four stat cards | `flex:1`, `#FCFAF6`, radius 12, `18px 20px`, shadow `.05`, 18px apart | `padding=18px 20px; borderRadius=12px`, `gap:18px` | match | A size down from `2b`'s: four cards over a grid, not over a table. |
| 5 | Stat figure | `500 24px` mono, tabular, 5px under the eyebrow; in `#36684D`, `#A44631`, `#1C1A15` | `k-stat-figure--sm`: `fontSize=24px; marginTop=5px` in the income / expense / ink tones | match | |
| 6 | Overdue card | `#F4E9DC`, radius 12, no shadow; eyebrow and figure `#8E4718`, then `400 12px` "items - post or reschedule" | `k-stat-card--warm`: `backgroundColor=rgb(239,227,214); boxShadow=none`, type in `--k-banner-btn-ink` | deviation | `#EFE3D6` (`--k-surface-warm-strong`), a shade off the artboard's `#F4E9DC`: it is the warm surface the selection bar and the import warning already use, and a fourth sand would be a token nothing else wants. |
| 7 | Overdue strip | one line: `12px 18px` on `#F4E9DC`, radius 10, 16px gaps, a 19px `#8E4718` glyph, then each item, then the button | `padding=12px 18px; backgroundColor=rgb(239,227,214); borderRadius=10px; gap=16px` | match | It used to hang off the day-1 cell, where an item three weeks late was invisible until you clicked a day it had nothing to do with. |
| 8 | An item on that strip | `500 12.5px` `#4A2A0F` "Overdue since 28.08 - Ubezpieczenie OC" and `500 12.5px` mono `#A44631` beside it | `k-overdue-text`: `fontSize=12.5px; fontWeight=500; color=rgb(74,42,15)`; `k-overdue-amount`: `fontSize=12.5px; color=rgb(164,70,49); fontFamily="IBM Plex Mono"` | match | |
| 9 | Rule between items | 1x16 `#DDCBB4` | `k-overdue-rule` on `--k-warm-rule` (`#DDCBB4`) | match | |
| 10 | Strip button | `7px 14px` on `#1C1A15`, radius 8, `600 12px` `#F3EFE7`, reading "Post both" | `k-btn-ink` reading "Post 2" | match | The count rather than "both": there can be seven. |
| 11 | Calendar card | `#FCFAF6`, radius 12, 20px padding, shadow `.05`; grid beside a 400px sheet, 20px apart | `SECTION_CARD` at `!p-5`, `flex-1` beside `k-day-panel`, `gap:5` | match | |
| 12 | Weekday header | `600 9.5px/.14em` uppercase `#6E6656`, centred, 8px gutters, 10px under | the eyebrow face at `text-[9.5px]`, `gap:8px`, `mb-2.5` | match | |
| 13 | Day cell | min-height 84, radius 8, 1px `#EDE7DA`, `8px 9px`, on `#FCFAF6`, 8px gutters | `minHeight=84px; border=1px solid rgb(237,231,218); borderRadius=10px; padding=8px 9px; backgroundColor=rgb(252,250,246)` | deviation | Radius 10, not 8: `--k-cal-day` is the app's one day-cell radius and `1c`'s calendar widget draws it at 10. |
| 14 | Day number and net | number `500 14px` mono `#4A443A` (weekends `#6B6353`); net `500 12px` mono in green or red, 6px under | `text-sm` mono in `k-cal-num` / `MUTED`, net at `text-xs` with `mt-1.5` | match | The net drops its pennies unless there are any: thirty-one cells are read down a column, and `,00` on every one is noise in a box nine pixels wide. |
| 15 | Dots | 5px round, 3px apart, 6px under the net, one per thing happening | `width=6px; height=6px`, `gap:3px` | deviation | 6px is `--k-cal-dot`, the same dot `1c`'s calendar widget draws. |
| 16 | Today | 2px `#1C1A15` border and a `600 8.5px/.1em` `#9A4E1F` "Today" | `border=2px solid rgb(28,26,21)` and the eyebrow face at 9px in the accent | match | |
| 17 | Outside-month cell | 1px dashed `#E2DBCC` on `#F8F4EB` | `border=1px dashed rgb(237,231,218); backgroundColor=rgba(0,0,0,0)` | deviation | The hairline and the page ground, not a second sand: a filled cell for a day that is not in the month reads as a day. |
| 18 | Day sheet | 400px, `#FCFAF6`, radius 12, `22px 24px`, shadow `.05`, beside the grid | `width=400px; padding=20px; borderRadius=12px; backgroundColor=rgb(252,250,246)` | match | A drawer that slid in from the right covered the month it was about, so the one comparison the screen exists for could not be made while it was open. |
| 19 | Sheet head | "Tuesday 15 September" `500 17px` and a 19px close; then In / Out / Net 18px apart in `400 12px` with mono figures, over a 1px `#EDE7DA` rule | `CARD_TITLE` and a `k-hairline-bottom` totals row at `12px` | match | The weekday is spelled out and the month is in the genitive, which is the only grammatical form for a day plus a month in Polish. |
| 20 | Sheet item | 1px `#E2DBCC` box, radius 9, `12px 14px`, 12px gap; name `500 13.5px`, sub-line `400 11.5px` `#6B6353`, amount `600 13.5px` mono | `k-day-item`: `border:1px solid var(--k-border); border-radius:9px; padding:12px 14px; gap:12px` | match | The sub-line is account and category; the artboard also names the recurrence, which `PlannedOccurrence` does not carry - adding it is a service change. |
| 21 | Post, from the sheet | not drawn | a small `publish` button on any item that is due | deviation | An item listed in the sheet that could not be posted from it would send the reader back to the strip to do it. |
| 22 | Sheet foot | two buttons over a 1px `#EDE7DA` rule, 22px above and 18px of padding: "Add for 15.09" on ink and "Post this day" on sand | `k-day-panel-foot`: `marginTop=22px; paddingTop=18px; borderTop=1px solid rgb(237,231,218)` with one full-width ink button | deviation | "Post this day" would be a new action, which this plan's Scope puts out of bounds; every item in the sheet already carries the one that posts it. |
| 23 | Sample data | two overdue items, a month with planned rows and subscription charges | the same, after `scripts/seed.py` grew two already-late planned transactions | match | A seed in which nothing is ever late leaves the strip and the Overdue card unseen. |
